interface:
  display_name: "MonoMod Patch DLL"
  short_description: "Build MonoMod.Patcher .mm.dll patch projects."
  default_prompt: "Use $monomod-patch-dll to create a MonoMod.Patcher .mm.dll project for a target assembly."
